import 'package:flutter/material.dart';

abstract class AppColors {
  static const primary = Color(0xFF6366F1); // Indigo
  static const secondary = Color(0xFF8B5CF6); // Purple
  static const accent = Color(0xFF06B6D4); // Cyan
  static const accentPink = Color(0xFFEC4899); // Pink

  // Gradient colors
  static const gradientStart = Color(0xFF0F172A); // Slate 900
  static const gradientMiddle = Color(0xFF1E293B); // Slate 800
  static const gradientEnd = Color(0xFF334155); // Slate 700

  static const background = Color(0xFF0F172A);
  static const backgroundSecondary = Color(0xFF1E1E2E);
  static const cardBackground = Color(0xFF1E293B);

  static const textPrimary = Color(0xFFF1F5F9);
  static const textSecondary = Color(0xFFCBD5E1);
  static const textTertiary = Color(0xFF94A3B8);

  // Glow colors for effects
  static const glowPrimary = Color(0x406366F1);
  static const glowSecondary = Color(0x408B5CF6);
}
